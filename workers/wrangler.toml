# 信奥导航站 Worker 配置
# 使用 Cloudflare Wrangler 部署

name = "oierlink-submit"
main = "submit.js"
compatibility_date = "2026-07-12"

# KV 命名空间绑定
# 部署前需要在 Cloudflare 控制台创建 KV 命名空间，然后将 ID 填入下面
# [[kv_namespaces]]
# binding = "OIERLINK_KV"
# id = "你的KV命名空间ID"
